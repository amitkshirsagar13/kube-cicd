# Automate Cluster installation process

Install git first and clone the repository for installation process.

```
sudo yum install git -y
git clone https://github.com/amitkshirsagar13/kube-cicd.git
```
